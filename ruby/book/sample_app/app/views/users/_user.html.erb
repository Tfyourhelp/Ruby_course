<li>
  <%= gravatar_for(user, size:50) %>
  <%= link_to user.name, user%>
  <% if current_user.admin? && !current_user?(user) %> <%# ! là not %>
    <%= button_to "delete", user, method: :delete, data: { confirm: "You sure?" }%> 
  <% elsif !current_user.admin? && current_user?(user) %>
    <%= button_to "delete1", user, method: :delete, data: { confirm: "You sure?" }  %>  
  <% end %>
</li>
